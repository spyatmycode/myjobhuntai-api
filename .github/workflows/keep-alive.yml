name: Keep Alive
on:
  schedule:
    - cron: '*/13 * * * *' # Runs every 13 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -I ${{ secrets.RENDER_URL }}